package com.arcxp.commerce.models


enum class LocationType {
    HOME, WORK, PRIMARY, OTHER
}

enum class Status {
    Active, Disabled, Anonymized, PendingAnonymize
}

enum class Gender {
    MALE, FEMALE, NON_CONFORMING, PREFER_NOT_TO_SAY
}
